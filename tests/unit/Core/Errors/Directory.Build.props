<Project>

    <Import Project="$([MSBuild]::GetPathOfFileAbove('Directory.Build.props', '$(MSBuildThisFileDirectory)../'))" />

    <PropertyGroup>
        <RootNamespace>Paraminter.Associating.Semantic.Type.Apheleia.Errors</RootNamespace>
    </PropertyGroup>
    
</Project>